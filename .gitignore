node_modules/
dist/
.astro/
*.log
.env
.env.*
!.env.example

# local dev-scratch (screenshots, throwaway test drivers) — never part of the app or a commit
temp/

worker/dist/
public/schemes.css
src/build-id.ts
# Vendored Scalar bundles are build artifacts — EXCEPT our forked one, which we commit so the deployed
# bytes are reproducible + version-controlled (a fresh checkout/CI deploy must not 404 the /reference script).
public/vendor/*
!public/vendor/scalar
public/vendor/scalar/*
!public/vendor/scalar/standalone-suluk.js
